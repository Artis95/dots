#!/bin/bash

# pbar &
setbackground 
#compton --xrender-sync-fence --xrender-sync --backend glx --vsync opengl-swc -b --dbus &
compton --config ~/.config/compton.conf -b --xrender-sync-fence --xrender-sync & 
#pbarbatterywall &
#megabacksync
